## 接口文档

本接口用于处理音频数据，支持通过Base64编码的Opus音频数据或文本数据，并返回处理结果

### 基本信息

+ 接口地址: /api/v1/response/

+ 请求方式: POST

+ 请求格式: JSON

+ 响应格式: JSON

### 请求参数

| 参数名 | 类型   | 必填 | 说明                               |
| ------ | ------ | ---- | ---------------------------------- |
| method | string | 是   | 请求方法类型                       |
| data   | array  | 是   | 包含文本或Opus音频Base64编码的数组 |

### 请求示例

```json
{
    "method": "vad",
    "data": ["WAL5li+IRKBOI+6W4/wt0lkPwF4nMGiH6jUf4KjUp9BL9ZCr/kbr8nP6ctpzlQbgQA=="]
}
```

### 响应参数

响应体为 JSON 格式。

| 字段名         | 类型          | 示例值                                           | 描述                                                         |
| -------------- | ------------- | ------------------------------------------------ | ------------------------------------------------------------ |
| status         | String        | "success" \|“error”                              | 请求的总体状态                                               |
| asr_result     | String        | "你好，今天天气怎么样？"                         | 语音识别（ASR）模块输出的文本结果                            |
| llm_response   | String        | "今天北京晴，气温在20到25摄氏度之间，非常舒适。" | 大语言模型（LLM）根据asr_result生成的文本回复。              |
| tts_result     | Array<String> | ["base64EncodedString1", "base64EncodedString2"] | Base64编码的TTS音频数据数组。每个字符串元素是一段音频数据的base64编码。客户端需要先进行Base64解码才能播放或保存为音频文件。通常，数组长度代表音频的段落数，一般为1。 |
| message (可选) | String        | String                                           | (当 status 为 error 时出现)	当 status 为 "error"          |



### Postman示例

#### 1. 请求设置

+ Method: POST

+ URL: http://localhost:8080/api/v1/response/

#### 2. Body设置

选择 raw 和 JSON 格式，输入以下内容：

```
{
  "method": "text",
  "data": [
   		"你好，请问你是谁"
  ]
}
```



### 音频处理流程

1. 录制或获取音频数据

2. 使用Opus编码器编码音频

3. 将编码后的数据转换为Base64字符串

4. 将Base64字符串添加到请求的data数组中

5. 发送POST请求到接口

6. 解析响应并处理结果

#### 常见问题

Q: 音频数据太大怎么办？
A: 可以考虑分片发送或使用流式传输

Q: 响应时间过长怎么办？
A: 可以添加超时设置，或使用异步处理方式

Q: 如何提高识别准确率？
A: 确保音频质量良好，避免背景噪音，使用合适的采样率

#### 注意事项

1. Base64解码： 客户端在收到响应后，必须对 tts_result 数组中的每个字符串进行 Base64解码，才能还原出二进制的音频数据（通常是PCM或WAV格式）。

2. 性能考量： 由于该接口集成了三个模型，响应时间（Latency）可能较长，建议客户端设置合理的超时时间。

3. 分段音频： tts_result 返回数组，是为将来可能实现的流式音频或分段输出预留的可能性。目前通常数组里只有一个元素，代表一整段完整的回复音频。

4. 错误处理： 即使HTTP状态码为200，也需要检查JSON体中的 status 字段是否为 "success" 来判断业务逻辑是否成功。